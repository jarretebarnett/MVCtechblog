<div class="container">
    <div class="row">
        <div class="column" style="max-width: 35rem;">
            {{#each posts}}
                <h2>{{name}}</h2>
                <p>{{description}}</p>
            {{/each}}
        </div>
    </div>
</div>